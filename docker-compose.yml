services:
  app:
    build: .
    image: ia-current:latest
    environment:
      DISPLAY: :99
      TESSERACT_CMD: /usr/bin/tesseract
    ports:
      # Reuses the host port previously assigned to TeamSpeak HTTP.
      - "8082:6080"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
